import {breakpoints} from '@src/theme/config';

export class Breakpoint {
  static readonly 'sm' = breakpoints.sm;
  static readonly 'md' = breakpoints.md;
  static readonly 'lg' = breakpoints.lg;
  static readonly 'xl' = breakpoints.xl;
  static readonly '1.5xl' = 1400;
}
